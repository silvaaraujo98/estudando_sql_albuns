-- Determinar as Faixas (Tracks) que Nunca Foram Vendidas.
SELECT a.name,
       a.composer,
       a.genreid
FROM track AS a
LEFT JOIN invoiceline AS b ON a.trackid = b.trackid
WHERE invoiceid IS NULL;